{
	"name": "server",
	"version": "1.0.0",
	"main": "index.ts",
	"license": "MIT",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsx",
		"dev": "nodemon",
		"start": "node src/index.js"
	},
	"dependencies": {
		"nodemon": "^2.0.15"
	}
}
